#include "main.h"
#include <stdio.h>

/**
 * mul - It multiplies two integers
 * @a : The 1st integer
 * @b : The second integer
 * Return: 0 always
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
