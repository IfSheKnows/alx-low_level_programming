#include "main.h"

/**
 * _islower - checks for lowercase characters
 * @i: The character that has to be checked
 * Return: if lowercase then 1, if anything else then 0.
 */

int _islower(int i)

{
	if (i >= 97 && i <= 122)
	{

	return (1);
	}
	return (0);
}
