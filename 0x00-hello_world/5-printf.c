#include <stdio.h>
/**
 * main - A program that prints a line with the printf function
 * Return: 0 (Works fine)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
